{
  "name": "p2p-media-loader",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm --recursive run build",
    "build:es": "pnpm --recursive run build:es",
    "build:umd": "pnpm --recursive run build:umd",
    "build:umd-min": "pnpm --recursive run build:umd-min",
    "clean": "pnpm --recursive run clean",
    "pack-packages": "pnpm --recursive exec -- pnpm pack",
    "lint": "pnpm --recursive run lint",
    "prettier": "pnpm --recursive run prettier"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.59.2",
    "@typescript-eslint/parser": "^5.59.2",
    "eslint": "^8.39.0",
    "eslint-plugin-prettier": "^4.2.1",
    "rimraf": "^5.0.0",
    "typescript": "^5.0.2",
    "vite": "^4.3.2",
    "prettier": "^2.8.8"
  }
}
